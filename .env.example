# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Postgres — local: use localhost:5434, prod: use Neon pooled connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5434/agent_orchestrator

# Langfuse (optional — traces disabled if not set)
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_HOST=https://cloud.langfuse.com
